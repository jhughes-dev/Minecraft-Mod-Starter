org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true

# Mod properties
mod.id={{mod_id}}
mod.name={{mod_name}}
mod.version=1.0.0
mod.group={{package}}

# Dependencies are resolved per-version from versions/<ver>/gradle.properties
